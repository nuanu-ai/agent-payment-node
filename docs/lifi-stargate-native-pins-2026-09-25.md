# C2-09 native Stargate Ethereum to Base pin evidence

The saved read-only [paired capture](../tests/core/lifi-fixtures/lifi-stargate-native-paired-safe-20260925.json) records two safe headers and two eight-member EIP-1898 block-hash-pinned state batches. It made four physical JSON-RPC POSTs, seven including earlier attempts, starting around 2026-09-25 06:56:37 UTC. A separate [executable-selector follow-up](../tests/core/lifi-fixtures/lifi-stargate-native-executable-selector-20260925.json) made one additional EIP-1898 read-only POST per chain around 07:06:35 UTC, paced 759 ms apart. No wallet, signature, send, operation, or payment was involved.

| Chain | Safe block | Safe hash | Safe timestamp UTC | Native pool code Keccak |
| --- | ---: | --- | --- | --- |
| Ethereum | 26052766 | `0xc273ddf05ea9419078f7de501490d6c8d7f1b80d105c76b4bebf8e909df8c2e6` | 06:46:47 | `0x1d390e7a18e79e4d3c3402aba4453b8f6ef1f7dc9de5dc6fab5b5d88d723639a` |
| Base | 51765009 | `0xb4a719c69abd5799942ad6be592a1a06ff683843efc1ff187a49ecdf4c48eeae` | 06:56:05 | `0xc2021862bb6d1b44c6f4ec866c51979e9f95dfc4afd77e0ece9215bb38deff84` |

The two safe heads differ by 558 seconds because the chains have different safe finality points. This is a paired retrieval and two independent chain snapshots, not a simultaneous cross-chain state or a delivery proof. On each chain, the eight reads cover pool, TokenMessaging, LI.FI diamond and Stargate facet bytecode; `stargateImpls(13)` to the official native pool; diamond `facetAddress(0x14d53077)` to the pinned facet; pool `getAddressConfig()` including the official TokenMessaging address; and facet `tokenMessaging()` to that same address. The extra read checks `facetAddress(0xa6010a66)`, the executable `swapAndStartBridgeTokensViaStargate` selector, at each earlier canonical safe hash. Both responses map to the already code-hash-pinned facet. The [official mainnet contract registry](https://docs.stargate.finance/resources/contracts/mainnet-contracts) lists the Ethereum and Base pool and TokenMessaging addresses.

Admission is limited to LI.FI `stargateV2` Fast/Taxi, asset ID 13, native ETH on Ethereum chain 1 to native ETH on Base chain 8453. Both legs require fresh safe-block code and configuration verification, including both facet selectors, with an exact block-hash tag before prepare persists their deployment identities. Historical source and destination observation recheck their receipt blocks against those frozen identities. The destination pool pin is passed to the native OFTReceived proof; a mismatched mapping, code/configuration, block hash, recipient, GUID, amount, or source EID fails closed. Other native Stargate pairs remain unavailable.

This source/test checkpoint leaves current owner policy, fresh provider materialization, live source effect, Base destination delivery, APN receipt/recovery, and paid acceptance unproved. Any future effect needs a separately admitted current policy and explicit authorization.
